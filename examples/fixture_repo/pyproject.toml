[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "calendar-edge-fixture"
version = "0.1.0"
description = "Intentionally faulty date-boundary fixture for StateTrace Agent"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
